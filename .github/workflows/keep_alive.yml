name: Wake up Streamlit
on:
  schedule:
    - cron: '0 12 * * *' # Executa todo dia ao meio-dia
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -I https://regiaogaal.streamlit.app/
